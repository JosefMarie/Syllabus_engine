rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Syllabi collection rules
    match /syllabi/{syllabusId} {
      allow read, write: if true;
    }

    // User Reading Progress tracking rules
    match /userProgress/{userId} {
      allow read, write: if true;
    }
  }
}
